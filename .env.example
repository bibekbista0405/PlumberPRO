REACT_APP_API_URL=http://localhost:5000/api

# Optional — web push public key (see NOTIFICATIONS_SETUP.md). Must match
# VAPID_PUBLIC_KEY in backend/.env. Leave unset to disable push entirely.
# REACT_APP_VAPID_PUBLIC_KEY=
